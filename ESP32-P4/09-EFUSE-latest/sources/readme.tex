注意事项：
1）但凡要改 Block0 参数的描述，可能会有 4 处内容需要修改：EN/CN 寄存器文件， EN/CN Block0 的表格
2）Block 0 的内容只提供简单版本的参数描述，不用列举具体的参数值分别代表什么，不然的话很容易出现 block 0 和寄存器里的描述不一致
3）寄存器mapping表格和下方列表是一一对应的，也就意味着改了表格中的寄存器名字，列表中的名字也要修改
4）改了 TEX 格式的寄存器名称，那么 CSV 也要修改，不然以后就会出现不一致的情况